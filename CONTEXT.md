# 교원인사 지식지도 도메인 언어

이 문서는 코드와 화면에서 같은 뜻으로 써야 하는 핵심 용어를 기록한다.

## 관계 의미

두 지식 항목이 왜 연결되는지를 나타내는 계약이다. `relations.py`가 유효한 관계 유형,
대상 항목 제약, 상세 화면의 문구, 지도의 선 표현을 함께 소유한다.

- 공개 화면에서는 `근거 법령`, `관련 기한·수치`, `필요 서식`처럼 업무 의미를 표시한다.
- 단순한 선 색상이나 화면별 임시 그룹을 관계 의미로 취급하지 않는다.

## 학교급 계약

사용자가 선택할 수 있는 학교급과 공통 범위, 학교급별 공식 실무도우미를 묶은 계약이다.
`school_scope.py`가 기본값과 유효값을 소유하고 스키마와 화면이 이를 공유한다.

- `공통 항목`: 중등·초등 모두에서 보이는 지식 항목
- `학교급별 위치`: 선택한 실무도우미에서 해당 항목을 찾을 쪽수
- 학교급 선택이 실제로 바꾸지 않는 정보까지 바뀐다고 안내하지 않는다.

## 지식 항목

제도, 법령·조문, 수치·기한, 절차, 서식, 해석 등 사용자가 찾아보는 한 단위다.
공개 문구에서는 내부 구현 용어인 `노드`보다 `지식 항목` 또는 구체적인 업무 명칭을 사용한다.
